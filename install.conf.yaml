- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.zshrc: zshrc
    ~/.config/nvim:
      path: nvim/**
      glob: true
      create: true

- shell:
    - [git submodule update --init --recursive, Installing submodules]

